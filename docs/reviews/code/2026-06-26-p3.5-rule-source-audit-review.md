# P3.5 Rule Source Audit / Seed Rule Review

Date: 2026-06-26

Branch: `wbs/P3.5-rule-source-audit`

## Scope

Reviewed P3.5 implementation artifacts:

- `src/whisper_wfst/rule_source_audit.py`
- `scripts/audit_rule_sources.py`
- `tests/test_rule_source_audit.py`
- `tests/fixtures/rule_source_candidates.jsonl`
- `data/rule_source_candidates.sample.jsonl`
- `data/correction_rules_seed.csv`
- `docs/reports/probes/rule_source_audit.md`

## Findings

No blocking implementation defects found in the P3.5 scope.

Validated behavior:

- Final eval-derived rule candidates are disabled and excluded from safe-only seed CSV.
- Low-support candidates remain `review_optional` and are excluded from safe-only seed CSV.
- Number/PII and long script span candidates are disabled.
- Safe-only seed output uses the P3 `CorrectionRule` CSV schema.
- Report records provenance, support count, source wav, segment id, risk, decision, and reason.

## Residual Risks

- Current committed input is a small sample, not a full domain audit.
- P3.5 does not promote any rule to `obligatory`.
- Production rule acceptance still depends on later targetability and evaluation phases.

## Verification

Commands run:

```bash
uv run --group dev pytest -q
uv run --group dev ruff check .
git diff --check
```

Result:

- pytest: pass
- ruff: pass
- whitespace check: pass
